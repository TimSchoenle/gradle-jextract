/**
 * The task one library declaration turns into.
 *
 * <p>A build script does not create these. The plugin registers one per entry in the
 * {@code libraries} container and wires the task's properties to the declaration's, so a value set
 * on the declaration after registration still reaches the task.
 */
@org.jspecify.annotations.NullMarked
package de.timscho.jextract.task;
